{
  "name": "daita",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "dependencies": {
    "jest": "^26.0.1",
    "lerna": "^3.22.1",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "release": "lerna version --force-publish",
    "build": "tsc -b",
    "clean": "git clean -fX -d packages/**/dist",
    "rebuild": "npm run clean && npm run build",
    "test": "jest",
    "coverage": "jest --coverage",
    "prepublishOnly": "npm run rebuild"
  },
  "license": "MIT"
}

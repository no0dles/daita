{
  "name": "daita",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "dependencies": {
    "jest": "^26.0.1",
    "lerna": "^3.22.1",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "release": "npm run build && lerna version --force-publish && lerna publish from-git",
    "build": "tsc -b",
    "clean": "git clean -fX -d packages/**/dist",
    "rebuild": "npm run clean && npm run build",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "license": "MIT"
}

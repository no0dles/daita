{
  "name": "@daita/sqlite-adapter",
  "version": "0.2.48",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "description": "relational sqlite adapter for daita",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fixlint": "tslint -c tslint.json --fix src/**/*.ts",
    "prettier": "prettier 'src/**/*.ts' --write",
    "prepublish": "node ../../scripts/build-pkg-config.js"
  },
  "license": "MIT",
  "dependencies": {
    "@daita/common": "^0.2.48",
    "@daita/relational": "^0.2.48",
    "sqlite3": "^4.2.0"
  },
  "devDependencies": {
    "@daita/relational-test": "^0.2.48",
    "@types/jest": "^25.1.1",
    "@types/sqlite3": "^3.1.6",
    "jest": "^25.1.0",
    "ts-jest": "^25.1.0",
    "typescript": "~3.9.3"
  }
}

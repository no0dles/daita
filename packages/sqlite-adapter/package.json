{
  "name": "@daita-src/sqlite-adapter",
  "private": true,
  "version": "0.2.26",
  "description": "relational sqlite adapter for daita",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fixlint": "tslint -c tslint.json --fix src/**/*.ts",
    "prettier": "prettier 'src/**/*.ts' --write"
  },
  "license": "MIT",
  "dependencies": {
    "@daita/common": "^0.2.26",
    "@daita/relational": "^0.2.26",
    "sqlite3": "^4.2.0"
  },
  "devDependencies": {
    "@daita/relational-test": "^0.2.26",
    "@types/jest": "^25.1.1",
    "@types/sqlite3": "^3.1.6",
    "jest": "^25.1.0",
    "ts-jest": "^25.1.0",
    "typescript": "~3.9.3"
  }
}

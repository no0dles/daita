{
  "name": "@daita/pg-adapter",
  "version": "0.2.49",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "description": "relational pg adapter for daita",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fixlint": "tslint -c tslint.json --fix src/**/*.ts",
    "prepublish": "node ../../scripts/build-pkg-config.js",
    "prettier": "prettier 'src/**/*.ts' --write"
  },
  "license": "MIT",
  "dependencies": {
    "@daita/common": "^0.2.49",
    "@daita/relational": "^0.2.49",
    "pg": "^8.3.0",
    "pg-connection-string": "^2.1.0"
  },
  "devDependencies": {
    "@daita/relational-test": "^0.2.49",
    "@types/jest": "^25.1.1",
    "@types/pg": "^7.14.4",
    "jest": "^25.1.0",
    "ts-jest": "^25.1.0",
    "typescript": "~3.9.3"
  }
}

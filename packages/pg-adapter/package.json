{
  "name": "@daita-src/pg-adapter",
  "private": true,
  "version": "0.2.28",
  "description": "relational pg adapter for daita",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fixlint": "tslint -c tslint.json --fix src/**/*.ts",
    "prettier": "prettier 'src/**/*.ts' --write"
  },
  "license": "MIT",
  "dependencies": {
    "@daita/common": "^0.2.28",
    "@daita/relational": "^0.2.28",
    "pg": "7.14.0",
    "pg-connection-string": "^2.1.0"
  },
  "devDependencies": {
    "@daita/relational-test": "^0.2.28",
    "@types/jest": "^25.1.1",
    "@types/pg": "^7.14.3",
    "jest": "^25.1.0",
    "ts-jest": "^25.1.0",
    "typescript": "~3.9.3"
  }
}

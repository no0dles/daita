{
  "name": "@daita/relational-example",
  "private": true,
  "version": "0.0.1",
  "description": "examples and tests for relational and adapters",
  "scripts": {
    "build": "tsc -b",
    "test": "env DATABASE_URL=postgres://postgres:postgres@localhost jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fixlint": "tslint -c tslint.json --fix src/**/*.ts",
    "prettier": "prettier 'src/**/*.ts' --write"
  },
  "license": "MIT",
  "dependencies": {
    "@daita/common": "0.2.8",
    "@daita/relational": "0.2.8",
    "@daita/pg-adapter": "0.2.8",
    "@daita/sqlite-adapter": "0.2.8"
  },
  "devDependencies": {
    "@types/pg": "^7.14.3",
    "typescript": "~3.9.3",
    "@types/jest": "^25.1.1",
    "jest": "^25.1.0",
    "ts-node": "^8.9.1",
    "ts-jest": "^25.1.0"
  }
}

{
  "name": "@daita/http-adapter",
  "version": "0.2.13",
  "author": "Pascal Bertschi @no0dles",
  "publishConfig": {
    "access": "public"
  },
  "bugs": "https://github.com/no0dles/daita/issues",
  "dependencies": {
    "@daita/common": "^0.2.13",
    "@daita/http-client-common": "^0.2.13",
    "@daita/relational": "^0.2.13",
    "axios": "^0.19.0"
  },
  "devDependencies": {
    "@daita/http-server": "^0.2.13",
    "@types/jest": "^25.1.1",
    "jest": "^25.1.0",
    "ts-jest": "^25.1.0",
    "typescript": "~3.9.3"
  },
  "homepage": "https://github.com/no0dles/daita",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "clean": "rm -rf ./dist",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "fix-linting": "tslint -c tslint.json --fix src/**/*.ts",
    "prettier": "prettier 'src/**/*.ts' --write"
  },
  "types": "dist/index.d.ts"
}
